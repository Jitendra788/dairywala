import { PageCard } from "@/components/hamari/page-card";

export default function ClusterPage() {
  return (
    <PageCard title="Manage Cluster">
      <p className="text-sm text-muted">Collection centres / clusters.</p>
    </PageCard>
  );
}
